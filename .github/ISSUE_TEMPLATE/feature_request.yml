name: Feature Request
description: Suggest a new feature or improvement
title: "[Feature]: "
labels: ["enhancement"]
body:
  - type: input
    id: version
    attributes:
      label: copilot-context-mode version
      description: |
        Check your package.json for the version. Make sure you're on the latest before requesting — it may already be implemented.
        Also [search existing issues](https://github.com/DriveWealth/copilot-context-mode/issues) to avoid duplicates.
      placeholder: "e.g. 0.1.0"
    validations:
      required: true

  - type: textarea
    id: problem
    attributes:
      label: Problem
      description: What problem does this solve? What's the current limitation?
    validations:
      required: true

  - type: textarea
    id: solution
    attributes:
      label: Proposed solution
      description: How should it work? Include example usage if possible.
    validations:
      required: true

  - type: textarea
    id: alternatives
    attributes:
      label: Alternatives considered
      description: Any other approaches you've thought about?
    validations:
      required: false
